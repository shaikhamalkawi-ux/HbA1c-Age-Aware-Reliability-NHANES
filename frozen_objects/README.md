# Frozen model/calibration identity

The original source-frozen pooled OLS and residual-conformal calibration identities were verified privately against the locked archive.

Public-safe certificate:

- [`frozen_temporal_ols_conformal_certificate.json`](frozen_temporal_ols_conformal_certificate.json)

It records the deterministic 1,775/444 source roles, predictor order, scaling values, pooled-OLS coefficients, global and age-Mondrian q90/q95 values, and the SHA-256 identities of the original freeze JSON and source-role ledger.

No binary/private model object or participant-level role ledger is redistributed here. Historical FIS file identities are reported only as hashes in `derived_outputs/legacy_anfis_verification.csv`; the private original files are not required to interpret the public aggregate verification result.

See [scientific verification](../docs/SCIENTIFIC_VERIFICATION.md) and [input identities](../verification/evidence_inputs.json).
