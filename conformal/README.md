# Conformal uncertainty verification

The evidence hierarchy is unchanged:

1. **Primary:** global residual and age-Mondrian conformal intervals.
2. **Secondary:** recovered original CQR stored-output analysis.
3. **Exploratory only:** later fixed-configuration CQR.

The primary source-calibration quantiles, age-specific coverage, interval widths, and interval scores passed independent replay. Recovered original CQR interval arithmetic also passed from the archived original stored outputs; this does not claim that the unavailable original fitted CQR learner objects were reconstructed or refit.

Public aggregate outputs:

- [`derived_outputs/primary_interval_metrics.csv`](../derived_outputs/primary_interval_metrics.csv)
- [`derived_outputs/recovered_original_cqr_metrics.csv`](../derived_outputs/recovered_original_cqr_metrics.csv)
- [`frozen_objects/frozen_temporal_ols_conformal_certificate.json`](../frozen_objects/frozen_temporal_ols_conformal_certificate.json)

See [scientific verification](../docs/SCIENTIFIC_VERIFICATION.md) and [final checks](../verification/scientific_checks_final.json).
